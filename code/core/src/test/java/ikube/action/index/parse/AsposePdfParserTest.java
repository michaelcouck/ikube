package ikube.action.index.parse;

import ikube.AbstractTest;

import org.junit.Test;

/**
 * @author Michael Couck
 * @since 21.11.10
 * @version 01.00
 */
public class AsposePdfParserTest extends AbstractTest {

	public AsposePdfParserTest() {
		super(AsposePdfParserTest.class);
	}

	@Test
	public void parse() throws Exception {
		// AsposePdfParser pdfParser = new AsposePdfParser();
		// File file = FileUtilities.findFile(new File("."), new String[] { "pdf.pdf" });
		// byte[] bytes = FileUtilities.getContents(file).toByteArray();
		// OutputStream parsed = pdfParser.parse(new ByteArrayInputStream(bytes), new ByteArrayOutputStream());
		// logger.debug("Parsed : " + parsed);
		// String content = parsed.toString();
		// assertTrue(content.contains("Application"));
		// assertTrue(content.contains("affiliation"));
		// assertTrue(content.contains("paynent"));
	}

}
